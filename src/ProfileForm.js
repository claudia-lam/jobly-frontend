/**
 * form for updating a profile
 *
 * Props:
 * - update
 * - user
 *
 * State:
 * - formData
 *
 * RoutesList --> ProfileForm --> Alert
 *
 */

function ProfileForm({ update, user }) {
  return <p>Edit Profile Form</p>;
}

export default ProfileForm;
